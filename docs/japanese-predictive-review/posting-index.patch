--- a/src/dictionary_builder/token_array/token_array.hpp
+++ b/src/dictionary_builder/token_array/token_array.hpp
@@ -46,6 +46,7 @@
     BitVector postingsBits; // 0 then 1* for each term
 
 private:
+    std::vector<int> loadedPostingOffsets; // MinIME: index immutable loaded data only.
     static void write_u64(std::ostream &os, uint64_t v);
     static void write_u32(std::ostream &os, uint32_t v);
     static void write_i32(std::ostream &os, int32_t v);
--- a/src/dictionary_builder/token_array/token_array.cpp
+++ b/src/dictionary_builder/token_array/token_array.cpp
@@ -9,19 +9,29 @@
     wordCost.clear();
     nodeIndex.clear();
     postingsBits = BitVector{};
+    loadedPostingOffsets.clear();
 }
 
 std::vector<TokenEntry> TokenArray::getTokensForTermId(int32_t termId) const
 {
     // postingsBits stores: 0, then 1* for term0 tokens, 0, 1* for term1 tokens...
     // Our BitVector/SuccinctBitVector select0 is 1-indexed, while termId is 0-based.
+    int b, c;
+    if (!loadedPostingOffsets.empty()) {
+        if (termId < 0 || static_cast<size_t>(termId) + 1 >= loadedPostingOffsets.size())
+            return {};
+        b = loadedPostingOffsets[static_cast<size_t>(termId)];
+        c = loadedPostingOffsets[static_cast<size_t>(termId) + 1];
+    } else {
     const int p0 = postingsBits.select0(termId + 1);
     const int p1 = postingsBits.select0(termId + 2);
     if (p0 < 0 || p1 < 0)
         return {};
 
-    const int b = postingsBits.rank1(p0);
-    const int c = postingsBits.rank1(p1);
+    b = postingsBits.rank1(p0);
+    c = postingsBits.rank1(p1);
+
+    }
 
     std::vector<TokenEntry> out;
     out.reserve(static_cast<size_t>(std::max(0, c - b)));
@@ -161,5 +171,10 @@
                  static_cast<std::streamsize>(n * sizeof(int32_t)));
 
     t.postingsBits = readBitVector(ifs);
+    int offset = 0;
+    for (size_t bit = 0; bit < t.postingsBits.size(); ++bit) {
+        if (t.postingsBits.get(bit)) ++offset;
+        else t.loadedPostingOffsets.push_back(offset);
+    }
     return t;
 }
