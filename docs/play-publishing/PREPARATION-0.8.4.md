# Release preparation impact — 0.8.4

Scope: refresh public Google Play submission materials for the chosen app names
and package ID; add a current Console worksheet and source-rights evidence;
refresh real phone screenshots and distribute verified existing signed binaries.
No production code, dictionaries, ranking or signing identity changes.

Independent changes: screenshot harness/capture evidence; current listing/forms/
packaging. Existing runtime core and pinned Rime validation preceded this task's
debug build. Verify scripts, text encoding/limits, image pixels/dimensions, archive
allowlists/hashes, public privacy access and repository diff.

Screenshot examples are demonstrations, not held-out quality evidence. Debug
instrumentation does not establish signed-release latency; static 16 KB alignment
does not establish runtime support. Do not clear source rights from an annotation
license alone. Owner account/contact/age/rating decisions cannot be invented.
Exclude keys, device backups, serials and account tokens from public archives.
No hosted CI.
