# Check the completeness tradeoff against Google Zhuyin

September 25, 2026, before further phone observations. The single-preference
trial preserves first-eight conversation and essay whole/compatible coverage.
However, essay first-choice target coverage loses eight episodes and the chat
sample loses two while gaining five. Those losses include common abbreviated
phrases displaced by less common fully spelled words. Passing the first-page
gate is insufficient evidence to ship those new Space defaults.

Freeze a diagnostic phone plan: all distinct raw inputs losing first-choice
whole-target coverage in the broad/chat corpora; ten hash-selected first-choice
gains; eight hash-selected changed Space choices not in either set. Seed
`completeness-reference-20260925`. Do not filter these groups by desired Google
outcomes. Compare the accepted core baseline and single-preference trial with
Google's visible suggestions and actual Space. Classify alignment gains/losses
by stratum; this deliberately oversamples regressions and is not population
accuracy. Preserve all results, including conflicts with the hypothesis.

The existing paired instrumentation runs on the already audited fixture APK,
which predates the pronunciation-count update. Its MinIME observations are a
fixture/control only, not this trial or current-production evidence. Reference
judgments use actual Google output joined with the current baseline/trial core
outputs. No app build or production admission follows from these old fixture
MinIME results. Inspect every Google typed screenshot. Native/core verification
and final trial-payload testing remain separate.

Use the acknowledged exclusive phone window and `tools/phone-lease.ps1` mutex.
Restore the original APK, preferences, learning, IME and viewport, verify actual
display OFF, then explicitly release. No Google data clearing, publication,
tablet operation, or hosted CI.
