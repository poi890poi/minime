# Refresh candidate-application attribution after the admitted changes

September 25, 2026. Runtime `6bffd17` (publication `e89a337`), actual non-debuggable
test-signed APK SHA-256
`2037bfe2a3fe7888596f449aedd54b0f09853e6842f7a44dfd492f4c59942c16`.
No runtime edit is proposed in this measurement step. Candidate updates still
miss the release latency target in earlier replays, but the last application
profile predates completion-existence, explicit-Latin, pronunciation and
completeness changes. Do not optimize against stale percentage attribution.

Reuse the existing `ApplicationProfileTest` without changing its eight frozen
queries or sampling rule. Run the untraced application-cost diagnostic, then the
sampled profile on the accepted release-mode payload. Providers are computed
before the measured callback, with production LocalLearning, source assets and
optional Chinese packs. Font filtering, keyboard rendering and touch are absent.
Report this as isolated attribution, never end-to-end latency or release
acceptance. Reused inputs are development evidence, not a fresh holdout.

Inspect the largest current direct-child costs and their call paths before
freezing the next single-variable intervention. Previous rejected scheduling,
learning-key, font-page and layout trials stay rejected; a similar new trial
needs a distinct causal mechanism, rather than repeating a favorable subset.

Acquire explicit phone ownership and the shared lease, retain it through
restoration/readback and verified display OFF, then explicitly release. Do not
use the tablet or an emulator. No CI, store operation or release-version bump.
