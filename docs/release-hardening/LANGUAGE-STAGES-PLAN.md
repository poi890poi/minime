# Attribute the accepted build's candidate delay

September 25, 2026, before diagnostic runs. Test-tooling change only: add three
named entries to the existing touch replay, using its existing stage observer.
No production code, scheduling, model, preferences or corpus change is proposed.

Accepted runtime `6bffd17` / publication `e89a337` meets sampled raw/Space budgets
but exceeds the candidate p95 budget in Chinese, Taiwanese and Japanese. The
unhooked baseline does not identify which stage causes those tails. Reuse the
same frozen language-specific shard 0 in those three modes, both cadences, on the
same release-mode fixture APK. Rebuild only instrumentation with new entry points.

Use CandidateTimingProbe unchanged to record request-to-delivery, callback
application, displayability, learning lookup and render costs, alongside raw
editor/candidate submission records and aggregate provider costs. Every requested
raw spelling and superseded/undelivered request remains in local evidence. Join
request episodes by ordered identity and time, never only by repeated spelling.
Do not call a main-thread delivery timestamp worker completion. Request-to-delivery
includes the 8 ms schedule, worker queue and computation plus the main queue.
Provider counters are aggregate costs, not per-key tail decomposition.

Hooks perturb execution. These sessions diagnose location of work only; do not
compare their percentiles to the unhooked baseline as a speedup/regression or
count them toward final latency certification. First summarize all modes and
missing requests, then declare any narrower causal experiment before edits.

Require an explicit shared-phone acknowledgement and mutex ownership, cooled
starts, 300-second replay bounds, full original APK/preferences/IME restoration,
verified display OFF and explicit release. One 30-minute reservation may contain
only the runs that can fit their 18-minute worst-case start/cleanup allowance;
request another window if necessary. Never infer availability from task status.
No Play upload, hosted CI or final release artifact is involved.
