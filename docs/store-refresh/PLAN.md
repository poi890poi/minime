# Store refresh and reported A-key misses

Three independent scopes:
- Original vector icon redesign around the mini-me / keyboard character idea;
  update launcher and store graphics together, preserving legibility at small sizes.
- Richer real mixed-mode candidates and Rudy POI screenshots, including requested
  加年端社. Demonstration probes may choose screenshots but may never change source
  data, rankings or regression targets. Keep existing two-language mode boundaries.
- Investigate missing A taps using broad all-letter position and drift tests,
  distinguish raw touch delivery from candidate/English-word acceptance. No
  production fix until a reproducible cause is established. Existing inset spacers
  and overlap/gesture ownership are primary inspection boundaries.

Baseline: d23f9c0 / 0.8.0. Preserve dictionary rows, mode semantics, gesture actions,
learning/privacy and stable keyboard geometry. Report hardware/human limitations.
Run core and pinned desktop checks before builds, then device dispatch/integration
under an explicitly acknowledged SHINE handoff and the shared named mutex. Restore
preferences, IME and viewport and verify display OFF before releasing the window.

User steering: complete and deliver the A/L fix first; discuss icon design later.
Keep icon and screenshot drafts out of the production change and publishing kit.
