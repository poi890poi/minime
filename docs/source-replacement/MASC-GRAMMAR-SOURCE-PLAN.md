# MASC grammar evidence under fixed lexical eligibility

September 25, 2026, before evaluation. Alternative source-only experiment, not a
union with spaCy and not a context-count replacement. Accepted runtime `6bffd17`.
The previous turn made progress: rejected a typing regression, completed four
language timing sessions, corrected an English benchmark configuration defect,
and exposed grammar omissions in the tokenizer-only trial. No phone is reserved.

Regenerate the existing source-aligned MASC contraction audit from its pinned
tagged package and publisher text hashes. Preserve its exact whole-document
alignment and verbal-clitic rule; do not adjust tags or repair the 44 excluded
documents. Use every supported spelling in its inventory, intersected with the
same accepted base lexicon or English unigrams observed twice used in the spaCy
trial. Preserve all `valid` metadata bytes, frequencies, runtime and other assets.
Only the source for grammatical flags differs. Keep the proposal in artifacts.

First run the frozen GRAMMAR-REFERENCE-PLAN screen on EWT dev/test and GUM test.
Compare the same narrow annotation categories, per-source/genre and unique versus
occurrence coverage. Stop before pipeline evaluation if AUX coverage loses within
either EWT split or any GUM genre without equal-or-better category coverage there.
Do not offset missing conversation forms with another genre. Verbal and nonverbal
categories remain separate; neither is a categorical false-positive oracle.
Existing source evidence and all these references are already exposed development
material, not a fresh holdout or safe mixed-language-intent certification.

If this screen survives, run fresh matched-receipt English and broad/chat Chinese
pipeline comparisons against the accepted metadata. Require English-mode identity
and no first-eight/Space reference loss within each source/genre. Then assess
source-derived apostrophe acceptance, casing, valid bare-word protection and raw
recovery. Corpus labels never enter extraction. No per-word additions, thresholds,
admission-policy changes or source combination are allowed in this experiment.

This may still admit ambiguous name/noun-plus-auxiliary spellings. Report that
limitation rather than silently adding a part-of-speech restriction. Any rule
separating grammatical evidence from safe automatic typing intent is a separate
behavior proposal. No rights gate, phone latency or public release is cleared by
passing a grammar-source screen; context counts retain their separate dependency.
